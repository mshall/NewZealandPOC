apiVersion: v1
kind: Service
metadata:
  name: nz-poc-svc
  namespace: cicd-test
spec:
  type: NodePort
  selector:
        app: nz-poc-deploy
        env: nz-po
  ports:
  - name: nz-poc-port
    port: 9090
    targetPort: 8080
